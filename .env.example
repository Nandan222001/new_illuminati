# Optional. Fixes the seeded Keeper (admin) credentials at build time.
# When VITE_ADMIN_PASSWORD is empty, a random demo passphrase is generated
# per browser on first load and shown on the /login page.
VITE_ADMIN_EMAIL=admin@illuminati.local
VITE_ADMIN_PASSWORD=
